# დემოს სცენარი

ეს დოკუმენტი აღწერს, რა უნდა აჩვენო დაცვის დროს და რა უნდა თქვა თითოეულ ეტაპზე. მიზანია demo იყოს მოკლე, გასაგები და სტაბილური.

## მომზადება დემომდე

გახსენით ორი terminal ფანჯარა.

### 1. Backend

```bash
cd /Users/ninucaaa/Desktop/new_project/bachelor-cardio-ai-project
scripts/run_backend.sh
```

შესამოწმებელი მისამართი:

```text
http://127.0.0.1:8765/docs
```

### 2. Frontend

```bash
cd /Users/ninucaaa/Desktop/new_project/bachelor-cardio-ai-project
scripts/run_frontend.sh
```

გასახსნელი მისამართი:

```text
http://127.0.0.1:5173/
```

## დემოს ძირითადი სცენარი

### ნაბიჯი 1 - პროექტის მოკლე ახსნა

სათქმელი:

> პროექტი არის გულ-სისხლძარღვთა დაავადების რისკის პროგნოზირების decision-support პროტოტიპი. ის იყენებს კლინიკურ მონაცემებს, XGBoost მოდელს და SHAP ახსნადობას, რათა პროგნოზთან ერთად აჩვენოს რომელი ფაქტორები მოქმედებს შედეგზე.

ხაზგასასმელი:

- სისტემა არ სვამს დამოუკიდებელ დიაგნოზს.
- სისტემა არის დამხმარე ანალიტიკური ინსტრუმენტი.
- მომხმარებლისთვის შედეგი ნაჩვენებია როგორც სავარაუდო ICD-კოდირებული გულ-სისხლძარღვთა დიაგნოზის ჯგუფი.
- ძირითადი მოდელი აფასებს საერთო გულ-სისხლძარღვთა სიგნალს, subtype მოდელები კი ადარებს კონკრეტულ ჯგუფებს: ინფარქტი, გულის უკმარისობა, ინსულტი, არითმია, ჰიპერტენზიული დაავადება და კორონარული არტერიის დაავადება.
- მთავარი აქცენტია prediction + explainability + ქართულენოვანი პაციენტის ანგარიში.

### ნაბიჯი 2 - Swagger API-ის ჩვენება

გახსენით:

```text
http://127.0.0.1:8765/docs
```

აჩვენეთ endpoint-ები:

- `GET /health`
- `GET /features`
- `GET /sample-patient`
- `POST /predict`

სათქმელი:

> Backend აწყობილია FastAPI-ზე. Swagger დოკუმენტაცია ავტომატურად გენერირდება და აჩვენებს ყველა endpoint-ს, request/response schema-ებს და შესაძლებელს ხდის API-ის ხელით შემოწმებას.

### ნაბიჯი 3 - Frontend-ის გახსნა

გახსენით:

```text
http://127.0.0.1:5173/
```

აჩვენეთ:

- პაციენტის მონაცემების ფორმა
- წონა კილოგრამებში და სიმაღლე სანტიმეტრებში
- ავტომატურად გამოთვლილი BMI
- ლაბორატორიული და triage ველები
- დამატებითი სიმპტომების ტექსტური ველი
- პროგნოზის ღილაკი

სათქმელი:

> მოდელი ელოდება 140 feature-ს, ამიტომ demo-სთვის ვიყენებ რეალურ test-split პაციენტს როგორც baseline-ს. მომხმარებელი ცვლის მხოლოდ რამდენიმე გასაგებ clinical field-ს, ხოლო დანარჩენი feature-ები რჩება sample patient-ის მნიშვნელობებით.

### ნაბიჯი 4 - პირველი პროგნოზი

დააჭირეთ:

```text
პროგნოზის გაშვება
```

აჩვენეთ:

- ყველაზე ძლიერი სავარაუდო დიაგნოსტიკური მიმართულება
- probability და დიაგნოსტიკური ზღვარი
- მოკლე განმსაზღვრელი ფაქტორები
- დაავადების ჯგუფების შედარება
- ექიმისთვის გადასამოწმებელი პუნქტები

სათქმელი:

> პროგნოზი ბრუნდება XGBoost მოდელებიდან. სისტემა აჩვენებს, რომელ დიაგნოზის ჯგუფთან არის ყველაზე ძლიერი სიგნალი და რომელი პაციენტის მონაცემები განსაზღვრავს ამ სიგნალს. ეს არის სავარაუდო კლინიკური მიმართულება და არა ექიმის საბოლოო დიაგნოზი.

### ნაბიჯი 5 - ველების შეცვლა

შეცვალეთ რამდენიმე ველი:

- ასაკი
- წონა ან სიმაღლე, რომ BMI შეიცვალოს
- სისტოლური წნევა
- გლუკოზა
- დამატებითი სიმპტომები

შემდეგ ისევ დააჭირეთ:

```text
პროგნოზის გაშვება
```

სათქმელი:

> ცვლილებების შემდეგ frontend აგზავნის ახალ payload-ს backend-ში. backend ამოწმებს feature-ებს, მოდელი აბრუნებს ახალ probability-ს, ხოლო SHAP ხელახლა ითვლის მნიშვნელოვან ფაქტორებს კონკრეტული input-ისთვის.

### ნაბიჯი 6 - პაციენტის ანგარიშის ჩვენება

აჩვენეთ პაციენტის ანგარიშის ბლოკი და კოპირების ღილაკი.

სათქმელი:

> აპლიკაცია ქმნის მოკლე ქართულ ანგარიშს, სადაც წერია პაციენტის პროფილი, სავარაუდო მთავარი მიმართულება, განმსაზღვრელი ფაქტორები და რა უნდა გადაამოწმოს ექიმმა. ეს ანგარიშიც decision-support ფორმატშია და არ ცვლის ექიმის დასკვნას.

### ნაბიჯი 7 - მოდელის ტექნიკური შეფასება

გახსენით მოდელის ტექნიკური შეფასების ფანჯარა.

სათქმელი:

> აქ ჩანს მოდელის test metrics: AUC, Recall და Precision. კლინიკური მომხმარებლის მთავარ ეკრანზე ეს მონაცემები დამალულია, რადგან შედეგი უნდა იყოს მოკლე და გასაგები, მაგრამ კომისიისთვის ჩანს როგორ შეფასდა მოდელი.

### ნაბიჯი 8 - შეცდომის დამუშავების ახსნა

Swagger-ში შეგიძლიათ ახსენოთ:

> თუ `/predict` endpoint-ზე არ გაიგზავნა ყველა საჭირო feature, backend აბრუნებს HTTP 422 შეცდომას და აჩვენებს რომელი feature-ები აკლია. ეს იცავს სისტემას არასწორი input-ისგან.

## 20-25 წუთიანი პრეზენტაციის სტრუქტურა

### Slide 1 - სათაური

- პროექტის დასახელება
- სტუდენტის სახელი
- ხელმძღვანელი

### Slide 2 - პრობლემა

- გულ-სისხლძარღვთა დაავადებები მაღალი სიკვდილიანობის მიზეზია.
- ექიმს უწევს მრავალი კლინიკური პარამეტრის ანალიზი.
- საჭიროა დამხმარე სისტემა, რომელიც სწრაფად აფასებს რისკს.

### Slide 3 - მიზანი

- ML-ზე დაფუძნებული diagnosis-support prototype.
- decision-support, არა დამოუკიდებელი დიაგნოზი.
- სავარაუდო დიაგნოზის ჯგუფი + განმსაზღვრელი ფაქტორები.

### Slide 4 - მონაცემები

- `546,028` ჩანაწერი
- `target_cvd`
- train/validation/test split
- no patient leakage across splits

### Slide 5 - Preprocessing

- outlier handling
- missing indicators
- median imputation from train split
- identifiers removed

### Slide 6 - მოდელები

- Logistic Regression
- Random Forest
- XGBoost

### Slide 7 - შედეგები

Final time-aware XGBoost test metrics:

- AUC-ROC: `0.8874`
- F1-score: `0.8323`
- Recall/Sensitivity: `0.8139`
- Precision: `0.8515`

### Slide 8 - Explainable AI

- რატომ არის ახსნადობა საჭირო
- პაციენტზე მორგებული განმსაზღვრელი ფაქტორები
- რა უნდა გადაამოწმოს ექიმმა

### Slide 9 - არქიტექტურა

აჩვენეთ flow:

```text
Dataset -> Preprocessing -> Training -> Saved Model -> FastAPI -> Vite JavaScript UI
```

### Slide 10 - Demo

- Swagger
- frontend
- prediction
- სავარაუდო დიაგნოზის ჯგუფი
- პაციენტის ანგარიში

### Slide 11 - შეზღუდვები

- retrospective hospital data
- high missingness
- ICU timing bias
- not clinically validated
- not final diagnostic system

### Slide 12 - მომავალი განვითარება

- early-screening model
- Docker
- external validation
- better UI
- patient history storage

## შესაძლო კითხვები და პასუხები

### კითხვა: რატომ XGBoost?

პასუხი:

> XGBoost კარგად მუშაობს tabular clinical data-ზე, უმკლავდება nonlinear ურთიერთობებს და validation/test შედეგებით აჯობა Logistic Regression-სა და Random Forest-ს.

### კითხვა: რატომ არ არის ეს დიაგნოსტიკური სისტემა?

პასუხი:

> რადგან მონაცემები არის რეტროსპექტული და სისტემა არ არის კლინიკურად ვალიდირებული რეალურ გარემოში. ამიტომ სწორია მისი აღწერა როგორც სავარაუდო დიაგნოსტიკური decision-support prototype და არა ექიმის ჩანაცვლება.

### კითხვა: როგორ დაამუშავეთ missing data?

პასუხი:

> არ წავშალე missing rows, რადგან ეს დიდ bias-ს გამოიწვევდა. დავამატე missing indicator სვეტები და რიცხვითი მნიშვნელობები შევავსე train split-ის მედიანებით, რათა validation/test leakage არ მომხდარიყო.

### კითხვა: რატომ sample patient frontend-ში?

პასუხი:

> მოდელს სჭირდება 140 feature. ყველა feature-ის ხელით შეყვანა demo-სთვის არაპრაქტიკულია. sample patient approach ინარჩუნებს სრულ model payload-ს და ამავე დროს აძლევს მომხმარებელს გასაგები კლინიკური ველების შეცვლის საშუალებას.

### კითხვა: რა არის SHAP?

პასუხი:

> SHAP არის explainable AI მეთოდი, რომელიც აჩვენებს თითოეული feature-ის გავლენას კონკრეტულ პროგნოზზე. ის ეხმარება მომხმარებელს გაიგოს, რატომ მიიღო მოდელმა კონკრეტული შედეგი.

### კითხვა: რა არის პროექტის მთავარი შეზღუდვა?

პასუხი:

> მთავარი შეზღუდვა არის ის, რომ მონაცემები მოდის ჰოსპიტალური ჩანაწერებიდან და არა რეალური outpatient screening გარემოდან. ამიტომ პროექტი არის პროტოტიპი და არა კლინიკურად დამტკიცებული სისტემა.

## Backup demo გეგმა

თუ frontend არ გაიხსნა:

1. გახსენით Swagger:

```text
http://127.0.0.1:8765/docs
```

2. აჩვენეთ `/health`.
3. აჩვენეთ `/features`.
4. აჩვენეთ `/sample-patient`.
5. ახსენით, რომ `/predict` იღებს სრულ feature payload-ს.

თუ backend არ გაეშვა:

1. აჩვენეთ `reports/model_metrics.md`.
2. გაუშვით:

```bash
.venv/bin/python src/predict.py --row-index 0 --top-n 6
```

3. აჩვენეთ CLI პროგნოზი და SHAP factors.
